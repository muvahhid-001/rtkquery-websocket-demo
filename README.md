# 🌐 RTK Query + WebSocket — Демо‑проект

Демонстрационный проект, показывающий, как интегрировать **WebSocket** с **RTK Query** (`onCacheEntryAdded`) в **React + TypeScript**.

---

## 🚀 Быстрый старт

```bash
# 1️⃣ Установите зависимости
npm install
# 2️⃣ Запустите демо‑сервер WebSocket (порт 4000)
npm run start:server
# 3️⃣ Запустите фронтенд (Vite)
npm run dev
Откройте в браузере: 👉 http://localhost:5173

💡 Что демонстрируется
🔄 Жизненный цикл подключения WebSocket через RTK Query (onCacheEntryAdded)

📡 Состояния соединения:

Подключение
Открыто
Ошибка
Закрыто

🔁 Автоматическое переподключение (простая стратегия backoff)
💬 Отправка и получение сообщений в чате
🫀 Обработка серверных событий:

welcome — приветственное сообщение
heartbeat — пульс сервера
chat — сообщения пользователей

📝 Как тестировать

При запуске сервера и фронтенда вы увидите последовательность:
🔌 Добро пожаловать, клиент #1 (15:56:12) - после обрыва соединение, заходишь как новый клиент.
❤️ Пульс сервера (15:56:17) - каждые 5 секунд
💬 Вы: Привет! (15:56:20) - если отправили сообщение!

Проверка ошибок
❌ Ошибка соединения Измените URL сервера на ws://localhost:9999 → появится сообщение об ошибке.
❌ Закрытие сервера Сервер сам закроет соединение через 15 секунд → появится уведомление об отключении.

Проверка чата
Используйте форму отправки сообщений → ваши сообщения транслируются всем подключенным клиентам.

📂 Структура проекта
Код
src/
├─ features/
│  └─ websocket/
│     ├─ components/
│     │  ├─ MessageList.tsx       # Список сообщений
│     │  ├─ MessageInput.tsx      # Форма отправки сообщений
│     │  └─ WebsocketStatus.tsx   # Отображение состояния подключения
│     ├─ websocketApi.ts          # RTK Query endpoint для WebSocket
│     └─ types.ts                 # Типы сообщений WSMessage
├─ App.tsx

💡 Как ориентироваться в коде

websocketApi.ts
Создает подключение к WebSocket через RTK Query.
Использует onCacheEntryAdded для подписки на события и обновления кэша.

MessageList.tsx
Отображает все входящие сообщения.
useState хранит массив сообщений.
useEffect добавляет новые сообщения при изменении data.
switch (msg.type) определяет формат отображения.

MessageInput.tsx
Форма для отправки сообщений.
Отправляет данные в формате:
json
{ "type": "chat", "payload": { "message": "string" } }

WebsocketStatus.tsx
Показывает текущее состояние соединения (connecting / open / error / closed).
Отображает последнее сообщение сервера.

🧪 Что проверять в демо
✅ Старт сервера и фронтенда → welcome и heartbeat
✅ Ошибка соединения → сообщение об ошибке
✅ Закрытие сервера → уведомление об отключении
✅ Отправка сообщений → появление в общем чате

⚡ Полезные советы
switch (msg.type) — ключ к обработке различных типов сообщений
useEffect([data]) — обновление состояния при поступлении новых сообщений
Тип WSMessage помогает TypeScript проверять корректность payload
SCSS Modules используются только для стилизации
```
